kind: PersistentVolumeClaim
apiVersion: v1
metadata:
  name: jaime-pvc
spec:
  accessModes:
    - ReadWriteOnce
  resources:
    requests:
      storage: 2Gi
  storageClassName: ocs-storagecluster-cephfs
  volumeMode: Filesystem
