apiVersion: v1
kind: Service
metadata:
  name: jaime-agent-kubernetes
  labels:
    application: jaime-agent-kubernetes
    component: agent
spec:
  ports:
    - port: 7001
      name: agent
  selector:
    app: jaime-agent-kubernetes
